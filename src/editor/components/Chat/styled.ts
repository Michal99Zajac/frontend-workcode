import styled from '@emotion/styled'
import { PopoverBody } from '@chakra-ui/react'

export const Styled = {
  PopoverBody: styled(PopoverBody)`
    max-height: 400px;
    min-height: 400px;
    overflow: auto;
  `,
}

export default Styled
